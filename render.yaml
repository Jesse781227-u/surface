services:
  - type: web
    name: surface
    runtime: static
    branch: main
    buildCommand: mkdir -p dist && cp index.html dist/index.html && cp -R audio dist/audio && cp -R assets dist/assets
    staticPublishPath: dist
    autoDeploy: true